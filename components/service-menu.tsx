import {groups,services,servicePrice} from '@/app/services';
export default function ServiceMenu(){return <div className="price-grid">{groups.map((group,i)=><article className={`price-card ${group.tone}`} key={group.id}><div className="card-top"><span>0{i+1}</span><span>XILE / SKIN CARE</span></div><h3>{group.title}</h3><p className="card-line">{group.line}</p><div className="price-from"><small>TỪ</small><strong>{group.from}</strong><span>đ</span></div><p className="card-description">{group.description}</p><ul>{services.filter(s=>s.group===group.id).map(s=><li key={s.id}><a href="#dat-lich" data-service={s.id}><span>{s.name}</span><strong>{servicePrice(s)} <i aria-hidden>↗</i></strong></a></li>)}</ul><a className="button" href="#dat-lich" data-service={services.find(s=>s.group===group.id)?.id}>Tư vấn {group.title.toLowerCase()} <span aria-hidden>↗</span></a></article>)}</div>}
